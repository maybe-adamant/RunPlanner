# Native conformance contacts

## Purpose and boundary

This audit maps every blocking fixed-route conformance check to the native Hades II
state or query that owns the observed result. Its purpose is to prevent a
reader from reconstructing game state from the execution plan, copying an
expected value into the observed result, or reading a nearby field that looks
equivalent but is not the value mutated by the game.

This file covers the three blocking structural conformance boundaries:

1. room-entry structure: room identity, incoming reward, encounter, and fixed
   room features;
2. outgoing Doors: normal and additional destinations and their rewards;
3. named room-exit state: the nine active facts, including element counts.

Immediate Timeline contacts such as a selected trait row or Well purchase are
covered by their action-family audits in this directory. They may report an
adapter mismatch at their native callback, but they are not alternate readers
of the named room-exit state below.

## Reader invariant

For a mutable fact, the observed value must come from the same native field or
query the game mutates or consumes:

- use the game's semantic query when the raw object is not the semantic value;
  trait level therefore uses `GetTraitCount(CurrentRun.Hero, { Name = key })`,
  not the first matching trait object's `StackNum`;
- use a direct field when that field is itself the game's ledger, such as
  `CurrentRun.RewardPriorities` or `BoonGrowthBoon.CurrentRoom`;
- use expected data only to select the bounded projection or retain an identity
  the game does not store, such as a planner acquisition identity;
- never copy an expected mutable charge, clock, rarity, level, duration, or
  status into the observed result;
- when native cleanup destroys the source object, immutable history may be
  retained from the expected row only after native state proves that the live
  source is gone.

The comparison remains exact over the published projection. Traits the planner
does not model are intentionally ignored rather than compared as a complete
native inventory.

## Structural checkpoints

| Checkpoint           | Published meaning                                                                                                                                               | Native authority read                                                                                                                                                           | Game contact and timing                                                                                                                    | Disposition                                                                                                                                                                                                                                                                                             |
| -------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Current room         | The next configured occurrence was entered.                                                                                                                     | The room passed to `StartRoom`, using `GenusName` and then `Name`.                                                                                                              | `StartRoom` after native room creation.                                                                                                    | Exact game name; the executor's occurrence stamp is correlation only.                                                                                                                                                                                                                                   |
| Incoming reward      | The entered room carries the authored incoming reward, or retains a native required boss reward.                                                                | `CurrentRun.CurrentRoom.ChosenRewardType`; native-required rewards require a non-empty value.                                                                                   | After the wrapped native `StartRoom`, so `SetupRoomReward` and `ChooseRoomReward` have settled it.                                         | Logical room acquisitions such as `InfernalContractBoon` are not physical reward pickups and are excluded.                                                                                                                                                                                              |
| Encounter            | The room's declared encounter phases were realized.                                                                                                             | The native room's `Encounter`/`Encounters` identities and phase binding.                                                                                                        | Room-entry proof after native `StartRoom`.                                                                                                 | Exact only for the published phase surface; no enemy-wave reconstruction.                                                                                                                                                                                                                               |
| Required object      | Each authored native obstacle exists.                                                                                                                           | `GetIdsByType({ Name = key })`.                                                                                                                                                 | Room-entry proof after room setup.                                                                                                         | Native object registry is authoritative.                                                                                                                                                                                                                                                                |
| Resources            | Each room/family point follows its published `native`, `suppress`, or `force` disposition; the resulting five counters match the planner at ordinary room exit. | Point-success fields on the native room plus the one `RandomChance` reached by `GrantElementFromTool`; counters from `CurrentRun.Hero.Elements`.                                | Point disposition is applied during native room setup; exact counters use the same room-exit conformance boundary as other retained state. | `native` preserves the physical point but forces its element roll to fail; `suppress` has no point; `force` preserves the point and forces success. A contribution gathered while leaving one room is observed at the next authored room's exit. The adapter never derives lookback or capacity policy. |
| Room objects         | Well, Pool, Shop, rack, and fountain presence matches Overview.                                                                                                 | Declaration-backed native room fields (`WellShop`, `SellTraitShop`, `StoreDataName`) or an active obstacle's `OnUsedFunctionName`.                                              | Room-entry proof after native setup.                                                                                                       | Presence only; inventory content has its own native feature adapter.                                                                                                                                                                                                                                    |
| Normal Doors         | Count, destination room, reward, provider source, and shared store match the published Doors product.                                                           | `MapState.OfferedExitDoors`, collapsed with native `CollapseTableOrdered`; each door's `Room`/`RoomData`, reward fields, and `ForceLootName`; `CurrentRun.NextRewardStoreName`. | Immediately before leaving the room, after `DoUnlockRoomExits`.                                                                            | Physical door order is preserved. A native-required boss reward is checked for presence rather than renamed.                                                                                                                                                                                            |
| Additional exits     | Each Chaos/Contract exit belongs to its published owner and destination.                                                                                        | The same offered-door set plus executor stamps installed when the native room is assigned to the door.                                                                          | Immediately before leaving the room.                                                                                                       | The stamps are correlation metadata; native room name and physical presence are still checked.                                                                                                                                                                                                          |
| Timeline obligations | Every published required transaction whose deadline has arrived completed.                                                                                      | The room session's bound/completed handle ledger.                                                                                                                               | Lifecycle checkpoints and final room close.                                                                                                | This is executor-owned transaction evidence, not reconstructed game state. Named carry-state proof remains separate.                                                                                                                                                                                    |

## Named room-exit facts

Room-exit conformance runs immediately before the wrapped native `LeaveRoom`.
At that point the room's player actions and native encounter-end effects have
settled, while the next room has not begun mutating run state.

| Fact               | Planner projection                                                                                            | Exact native authority                                                                                                                                                                                                                                                                     | Source evidence and notes                                                                                                                                                                                                                                                                                         |
| ------------------ | ------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `traitInventory`   | Only named expected presences/removals, with published rarity, level, and Hammer rank fields.                 | Presence and rarity from `CurrentRun.Hero.Traits`; level from `GetTraitCount(CurrentRun.Hero, { Name = key })`; Hammer Rank II from `trait.Rarity == "Legendary"`.                                                                                                                         | `TraitLogic.lua:GetTraitCount` resolves the hero trait by name and returns its effective `StackNum`; `PowersLogic.lua:UpgradeHammers` upgrades Rank II Hammers by setting Legendary rarity. Unmodeled traits are deliberately ignored.                                                                            |
| `steadyGrowth`     | Steady Growth trait key, progress, and processed interval.                                                    | The live `BoonGrowthBoon.CurrentRoom` and `BoonGrowthBoon.RoomsPerUpgrade.Amount`.                                                                                                                                                                                                         | `TraitData_Demeter.lua:BoonGrowthBoon` declares both fields. `TraitLogic.lua:CheckChamberTraits` increments `CurrentRoom`, compares it with the processed `Amount`, resets it on maturity, and performs the rarity upgrade. `SteadyGrowthProgress` is not a native field.                                         |
| `chaos`            | Active curse/blessing pair, rarity, clock, remaining uses; matured blessing key and rarity.                   | Active curse traits and matured blessing traits in `CurrentRun.Hero.Traits`; pair from `curse.OnExpire.TraitData`; clock from `UsesAsEncounters`, `UsesAsRooms`, or the two god-screen curse identities; remaining count from `RemainingUses`.                                             | `UpgradeChoiceLogic.lua` attaches the processed blessing to the selected curse's `OnExpire.TraitData`; native trait-use processing decrements the curse's actual `RemainingUses`. A Transcendent Embryo blessing is intentionally also a matured Chaos blessing in the planner trait ledger.                      |
| `keepsakeEffects`  | The bounded mutable state for the eleven modeled keepsake effect families.                                    | See the keepsake matrix below.                                                                                                                                                                                                                                                             | Static `origin`, planner acquisition identity, and historical identity after native removal are the only expected-data carry-throughs.                                                                                                                                                                            |
| `rewardPriorities` | Ordered pending reward-pressure names.                                                                        | `CurrentRun.RewardPriorities` as an array.                                                                                                                                                                                                                                                 | `RunLogic.lua` initializes the list, `RewardLogic.lua:RewardStoreAddPriority` appends with `table.insert`, and `ChooseRoomReward` consumes it with `ipairs` and `RemoveValueAndCollapse`. A name-to-count map is not equivalent.                                                                                  |
| `pathOfStars`      | Spell trait, layout, frozen Rare/Epic/God Sent identities, closure, banked points, and invested points.       | `CurrentRun.Hero.SlottedSpell.TraitName`; `SlottedSpell.Talents.Name`; identities traversed from the native talent tree; `CurrentRun.AllSpellInvestedCache`, `NumTalentPoints`, and `InvestedTalentPoints`.                                                                                | `TalentScreenLogic.lua:TryCloseTalentTree` mutates invested nodes and both counters, then calls `SpellLogic.lua:UpdateTalentPointInvestedCache`. Common/repeatable nodes are outside the published projection.                                                                                                    |
| `forfeit`          | `inactive`, `available`, or `consumed` for the current biome.                                                 | Rank from `GetNumShrineUpgrades("BoonSkipShrineUpgrade")`; consumption from `CurrentRun.BiomeBoonSkipCount`.                                                                                                                                                                               | `ShrineLogic.lua:CheckBoonSkipShrineUpgrade` compares and increments the same counter; `RunLogic.lua` resets it at biome transition.                                                                                                                                                                              |
| `elementCounts`    | Absolute Aether, Earth, Air, Fire, and Water counts at the current room's complete `beforeRoomExit` snapshot. | `CurrentRun.Hero.Elements`, with absent keys treated as zero.                                                                                                                                                                                                                              | Published for every complete room-exit snapshot, including unchanged vectors. A contribution gathered during the prior room's native exit is therefore checked here; one gathered while leaving the current room is checked at the next authored occurrence. This is deliberately separate from `traitInventory`. |
| `stygianWell`      | Pending Spark, Yarn, Hymn, Extended, discount-duration, and empty-slot-duration uses.                         | The corresponding live traits in `CurrentRun.Hero.Traits`; `RemainingUses` for Spark/Yarn/Extended/durations and `Uses` for Hymn. Duration sign derives from `UsesAsBosses`: boss-clocked Extended purchases are negative in the planner projection, encounter-clocked purchases positive. | `TraitData_Store.lua` declares the fields. `StoreLogic.lua:RecalculateStoreTraitDurations` realizes encounter duration; `HandleStorePurchase` changes an Extended target to `UsesAsBosses` and assigns `BossExtension`. Copying the planner's sign without reading `UsesAsBosses` would conflate the two clocks.  |

`echoShopDuplicate` and `hermesShrineDeliveries` are valid decoded diagnostic
values but are not active room-exit conformance facts. Silently returning an
inferred value for either is not permitted.

## Keepsake subfields

| Effect                   | Mutable native authority                                                                                                                                                | Historical-only expected data                                                                                          | Evidence and disposition                                                                                                                                                                                                                   |
| ------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Olympian reward pressure | Source `trait.Uses`; `trait.RarityUpgradeData.Uses`; live processed `trait.RarityUpgradeData.MaxRarity`.                                                                | Provider, origin, acquisition order; rarity cap only after the native source has been fully removed.                   | `RewardStoreAddPriority` consumes force pressure; `UpgradeChoiceLogic:TryRarifyBoons` reads and consumes the processed rarity data.                                                                                                        |
| Jeweled Pom              | `GameState.FatedStatus`; `HadesAndPersephoneKeepsake.FatedBoonLevelBonus`; the one live trait marked `GrantedTrait`.                                                    | Acquisition identity; granted trait key only after Unfated cleanup removes the marked trait.                           | `PowersLogic.lua:GiveRandomHadesBoonAndBoostBoons` gates through Fated state and marks the grant; `KeepsakeLogic.lua` removes that marked trait on Unfated transition.                                                                     |
| Experimental Hammer      | Live traits with `IsHammerTrait`, `UsesAsEncounters`, and positive `RemainingUses`.                                                                                     | Planner acquisition identity used to correlate same-key temporary instances; absent older rows remain expired history. | `PowersLogic.lua:GiveDurationHammer` produces exactly these processed fields. Surplus live instances are emitted as unmatched evidence.                                                                                                    |
| Calling Card             | `RarifyKeepsake.RarityUpgradeData.Uses`.                                                                                                                                | None.                                                                                                                  | `UpgradeChoiceLogic:TryRarifyBoons` decrements the same value.                                                                                                                                                                             |
| Time Piece               | `GoldifyKeepsake.BoonConversionUses`.                                                                                                                                   | None.                                                                                                                  | `GiftLogic.lua` decrements this value when the target is destroyed.                                                                                                                                                                        |
| Fig Leaf                 | Every live `PersistentDionysusSkipKeepsake.RemainingUses` and `ActivatedThisBiome`; for the final removed use, `CurrentRun.CurrentRoom.TraitUses[keepsakeKey]`.         | None.                                                                                                                  | `EncounterLogic.lua` calls `UseHeroTraitsWithValue("SkipEncounterChance")` and then sets the latch if the trait remains. `TraitLogic.lua:LogTraitUses` records the final use on the room even when native cleanup removes the trait first. |
| Gorgon Amulet            | `AthenaEncounterKeepsake.RemainingUses`, its live `Slot`, and `CurrentRun.ExpiredKeepsakes`.                                                                            | None.                                                                                                                  | Native keepsake expiry records the specific key. Unslotted without consumption is the planner's expired state, not consumed.                                                                                                               |
| Aromatic Phial           | `FountainRarityKeepsake.Uses`.                                                                                                                                          | None.                                                                                                                  | `InteractLogic.lua:UseHealthFountain` calls `ReduceTraitUses` before native `AddRarityToTraits`.                                                                                                                                           |
| Crystal Figurine         | `BossMetaUpgradeKeepsake.RemainingUses`; absence of that specific source after native cleanup.                                                                          | Origin, and rarity only after the source is gone.                                                                      | `EncounterLogic.lua:TriggerPostBossEvents` calls `UseHeroTraitsWithValue("PostBossCardRarity")`. `CurrentRun.TemporaryMetaUpgrades` is not valid consumption proof because it also contains unrelated temporary Arcana.                    |
| Concave Stone            | `UnpickedBoonKeepsake.Uses` and live rarity.                                                                                                                            | Origin.                                                                                                                | Native double-boon selection consumes the source's own `Uses`; no offer reconstruction is needed at room exit.                                                                                                                             |
| Transcendent Embryo      | `RandomBlessingKeepsake.Rarity` and `CurrentRoom`; the live trait marked `FromChaosKeepsake`; its exact processed operand fields read through the shared Chaos adapter. | Origin and planner acquisition identity.                                                                               | `PowersLogic.lua:AddRandomChaosBlessing` marks the blessing; `TraitLogic.lua:CheckChamberTraits` advances `CurrentRoom` and replaces that marked blessing at maturity.                                                                     |

## Regression standard

Focused conformance tests must vary expected and native values independently.
At minimum, they witness:

- a trait whose first object's raw `StackNum` differs from the value returned by
  native `GetTraitCount`;
- Steady Growth expected values that differ from live `CurrentRoom` and
  processed `RoomsPerUpgrade.Amount`, plus a missing source;
- an Olympian source whose live processed `MaxRarity` differs from expected;
- encounter-clocked and boss-clocked Well durations with the same positive
  native `RemainingUses`;
- an unrelated `TemporaryMetaUpgrades` entry that cannot prove Crystal
  Figurine consumption.

These are authority-mutation witnesses, not snapshots of convenient sample
data. A future reader refactor that copies expected mutable state should fail
them even when ordinary happy-path values happen to agree.
